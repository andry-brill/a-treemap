name: any_treemap_example
description: Example application for any_treemap.
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  any_treemap:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
